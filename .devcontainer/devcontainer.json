{
  "name": "deski-dev",
  "dockerComposeFile": [
    "../docker-compose.yml"
  ],
  "service": "backend",
  "workspaceFolder": "/app",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "postCreateCommand": "chown -R vscode:vscode /app && git config --global --add safe.directory /app && bash /app/.devcontainer/setup-env.sh && pip install --no-cache-dir -r /app/requirements.txt",



  "remoteUser": "vscode"
}
