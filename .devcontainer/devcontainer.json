{
  "name": "deski-dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "dockerComposeFile": [
    "../docker-compose.yml"
  ],
  "service": "backend",
  "workspaceFolder": "/app",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "postCreateCommand": "./.devcontainer/setup-env.sh && pip install --no-cache-dir -r requirements.txt","postCreateCommand": "[ -f .env ] || cp .env.template .env && git config core.fileMode false && ./.devcontainer/setup-env.sh && pip install --no-cache-dir -r requirements.txt",
  "remoteUser": "vscode"
}
