#
# This file is used to specify the dependencies for the project.
#
# Update this manually with HIGH LEVEL LIBRARIES, then run `pip-compile` to generate requirements.txt.
#

requests
fastapi
uvicorn
pydantic
dotenv
langchain-chroma
azure-ai-inference
langchain-huggingface
azure-core
azure-identity
langchain-community
langchain-text-splitters
requests-mock
pytest
python-multipart
ruff
sentence-transformers
numpy<2.0.0  # Needed because LangChain doesn't support numpy 2.x yet
torch>=2.1.0
